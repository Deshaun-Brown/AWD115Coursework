# Copilot Instructions

## Project Guidelines
- User prefers to use the Visual Studio Package Manager Console (PMC) for Entity Framework Core commands (e.g., Add-Migration, Update-Database) instead of the dotnet ef CLI. When database updates are needed, provide the PMC commands for the user to run manually instead of running CLI commands automatically.